**수정 리스트 (25/03/10)**
[전체 영역]

- 워크스페이스 항목의 이름을 센터로 변경

[대시보드]

- 카드 섹션 : 평가 건수, 평균 평가 점수 상세화
- 최다 감점 항목 Top5, 최다 감점 콜 Top5 항목 추가
- 항목 선택 시 모달창으로 처리 됐던 부분 페이지 이동으로 수정
- 각 이슈 및 그래프를 눌렀을 때 해당 테이블로 필터링 되어 이동.

[사이드 메뉴]

- 홈 > 통합 평가 현황 메뉴 추가
- 통합 테이블과 상세 분석을 탭으로 분리

[상세 분석 > 전체]

- 검색 조건 날짜로 통일 
- 카드 섹션 : 평가 건수, 평균 평가 점수, 상담사 평균 통화 시간 추가 및 상세화 
- 현황 테이블 컬럼 추가 : NO., 평균 통화시간

[상세 분석 > 전체 > 센터별]

- 검색 조건 날짜로 통일 
- 카드 섹션 : 평가 건수, 평균 평가 점수, 상담사 평균 통화 시간 추가 및 상세화 
- 현황 테이블 컬럼 추가 : NO., 평균 통화시간

[상세 분석 > 전체 > 팀별]

- 검색 조건 날짜로 통일 (나머지는 카드 섹션을 통해 검색)
- 카드 섹션 : 
  > 평가 상담사, 평가 건수, 긴급 이슈, 상담사 평균 통화시간, 평가 제외 상담사, 평균 평가 점수, 최다 감점 항목, 70점 이하 콜 항목 수정 및 추가 
  > 카드 영역을 눌러 해당 내용으로 테이블 검색이 될 수 있게 변경 
- 현황 테이블 컬럼 변경
  > NO., 상담사, 사번, 총 상담건수, 평균 통화시간, 최다 감점항목, 평균 점수, 이슈

[상세 분석 > 전체 > 팀별 > 상담사]

- 검색 조건 날짜로 통일 (나머지는 카드 섹션을 통해 검색)
- 카드 섹션 : 
  > 평가 건수, 긴급 이슈, 상담사 평균 통화시간, 평균 평가 점수, 최다 감점 항목, 70점 이하 콜 수정 및 추가
  > 카드 영역을 눌러 해당 내용으로 테이블 검색이 될 수 있게 변경 
- 현황 테이블 컬럼 변경
  > 평가 ID, 일시, 상담 유형, 유형, 평가방식, 평가표, 통화시간, 신뢰도, 점수, 이슈

[상세 분석 > 전체 > 팀별 > 상담사 > 평가 항목]

- 3컬럼으로 레이아웃 변경 : [왼쪽: 콜 정보] [중간: 스크립트+플레이어] [오른쪽: 항목별 점수]
- 콜 정보 추가
- 평가 항목 선택 시 해당 구간 플레이

[수동 검증 모달]

- 3컬럼으로 레이아웃 변경 : [왼쪽: 콜 정보] [중간: 스크립트+플레이어] [오른쪽: 항목별 점수]
- 콜 정보 추가
- 평가 항목 선택 시 해당 구간 플레이

**수정 리스트 (25/03/13)**
[대시보드]
최다 감점 콜 Top5 : 센터, 팀, 이름이 나오도록 수정

[트랜드분석 > 비교분석] (메뉴 명칭 변경)

- 센터 비교 / 팀 비교 유형 선택 (2~3개 대상 선택 가능)
- 팀 비교 시 센터별로 줄바꿈하여 그룹 정렬 (센터명 라벨 표시)
- 비교 위젯을 사이드 바이 사이드 카드 형태로 재설계
- 표시 항목 토글로 위젯 노출 제어
- 최고값 대비 차이(▼) 표시 규칙 적용 (높을수록 좋은 지표 / 낮을수록 좋은 지표 구분)
- 지표 영역과 숫자 영역을 3칼럼 grid로 분리하여 정렬 통일
- 팀별 차트 색상을 10개 모두 고유 색상으로 지정하여 구분성 확보
- 위젯 목록 :
  > 품질 점수 추이 : 전체 너비 라인 차트 (선택 대상 오버레이)
  > 평균 QA 점수 : 좌(전체 평균 + 전기간 대비) / 우(유형별 점수: 자동 I/B, 자동 O/B, 수동)
  > 상담 / 평가 건수 : 좌(총 평가 건수 큰 숫자) / 우(유형별 내역: 자동 I/B, 자동 O/B, 수동) + 스택 바
  > 이슈 발생률 : FAIL(금지어), 기준 미달 건수·비율 + 스택 바
  > 추세 및 편차 : 최고점, 최저점, 편차, 표준편차 + 범위 바
  > 상담사 평균 상담 시간 : 분/초 큰 숫자 + 프로그레스 바
  > 점수 분포 / 종합 성과 : 도넛 그래프(우수·양호·주의·미달) | 레이더 차트(QA 점수, 평가 완료율, 정상 비율, 우수 비율, 상담 효율) 5:5 배치
  > 최다 감점 항목 Top 5
  > 최다 감점 콜 Top 5
- 드릴다운 네비게이션 : 위젯 클릭 시 통합 평가 현황으로 필터링 이동
  > 품질 점수 추이 : 차트 포인트 클릭 → 날짜 + 센터/팀 필터
  > 이슈 발생률 : FAIL/기준 미달 행 클릭 → 이슈 유형 + 센터/팀 필터
  > 점수 분포 도넛 : 세그먼트 클릭 → 점수 구간 + 센터/팀 필터
  > 최다 감점 항목 Top 5 : 항목 행 클릭 → 감점 항목 + 센터/팀 필터
  > 최다 감점 콜 Top 5 : 콜 행 클릭 → 저점수 필터 + 센터/팀 필터

[시스템 설정 > FAIL 판정 규칙]

- 사이드 메뉴 시스템 설정 그룹에 'FAIL 판정 규칙' 메뉴 추가
- STT 기반 자동 FAIL 판정 규칙 관리 페이지 신규 생성
- 좌우 5:5 비율 그리드 레이아웃 : 금지어 | 필수 고지 사항
- 금지어 : STT에서 감지 시 즉시 FAIL 처리되는 키워드 관리 (욕설·비속어, 고객 비하, 경쟁사 비방, 허위 약속, 개인정보 요구, 통화 강제 종료 암시, 성희롱·차별 발언, 내부 정보 유출)
- 필수 고지 사항 : STT에서 미감지 시 FAIL 처리되는 키워드 관리 (녹취 동의 안내, 개인정보 수집·이용 동의, 본인 확인 절차, 상품 위험 고지, 청약 철회 안내, 불완전판매 방지 고지)
- 키워드 1개 = 1행 구조 (단일 키워드 입력 방식)
- 테이블 컬럼 : No(10%), 키워드(20%), 키워드 설명(25%), 상담 유형(15%), 상태(10%), 관리(20%) — 전 컬럼 중앙 정렬
- 상담 유형 : 전체 / I·B / O·B 선택
- 상태 : 토글 스위치로 활성/비활성 전환
- 관리 : 수정(연필 아이콘) / 삭제(휴지통 아이콘)
- 추가/수정 모달 : 키워드(STT 감지 문구) + 적용 상담 유형을 한 행 배치, 키워드 설명을 다음 행 배치

[분석 > 리포트 생성기]

- 리포트 유형 통합 : 일간, 주간, 월간, 연간, 날짜 지정으로 구체화하여 날짜 선택을 한번에 처리 (기간 및 범위 영역에서 날짜 입력 제거)
- 일간/주간/월간/연간은 오늘 기준 자동 계산, 날짜 지정만 from~to 직접 입력
- 템플릿 선택 영역 제거 (포함 섹션으로 커스터마이징 대체)
- 포함 섹션 항목 확장 (7개 → 15개) : 대시보드·비교분석 화면의 모든 위젯을 리포트 섹션으로 추가
- 포함 섹션 그룹 분류 : 개요 / 품질 분석 / 팀·상담사 / 이슈 및 감점 / 종합 5개 그룹으로 소제목+구분선 표시
- 요약 (KPI 현황) : 대시보드 4열 카드 스타일로 확장 (채널별 건수·완료율·점수, 전기 대비, 통화시간 증감 포함)
- 팀별 평가 현황 컬럼 확장 : 센터, 팀, 상담사 수, 상담 건수, 평가 건수, 완료율, 평균 점수, 평균 통화 시간, 이슈 (변화 컬럼 삭제)
- 부록 확장 : 점수 분포 수치 상세 테이블, 용어 정의/평가 기준 안내 테이블 추가

**수정 리스트 (25/03/17)**
[전체 영역]

- 데이터 정합성 체크 후 수정

[사용자 관리]

- PERM_TREE 구조 변경 : 데이터 범위 → 접근 가능 메뉴(관리자 영역 + 상담사 영역) 순서로 재배치
- 테이블 컬럼 분리 : 기존 "접근 범위" 단일 컬럼을 "데이터 범위" + "메뉴 권한" 2개 컬럼으로 분리
- 데이터 범위 표시 : 전체 / 센터 / 센터 > 팀 계층적 표시, 복수 시 첫 항목 + "외 N건"
- 메뉴 권한 표시 : 전체(초록 배지) / 일부 제한(주황 배지)
- 필터 섹션 개선 : "필터" → "검색조건" 라벨 변경, 접기/펼치기 토글 추가
- 상세 모달 디자인 개선 : 프로필 스타일 헤더(아바타 + 이름/이메일 + 접속 권한 배지) + 소속 정보 카드(센터/팀/가입일 3분할)
- 수정 모달 개선 : 하드코딩 센터/팀 옵션을 centerTeamMap 기반 동적 생성 + onEditWsChange 추가
- 등록 모달 개선 : 권한 트리 포함, 모달 크기 lg, saveNewUser에서 getCheckedPerms 저장
- 더미 데이터 보강 : 실제 팀 배정, 다양한 데이터 범위 권한(전체/센터/센터>팀/복수 센터 걸침)

[개선 이행 관리]

- 신규 페이지 생성 (pages/admin/improvement-manage.html)
- 사이드바 QA > 분석 그룹 하위에 메뉴 추가 (리포트 생성기 아래)
- 검색 필터 : 기간(시작/종료), 리포트 유형(전체/일간/주간/월간/연간), 담당자, 상태(전체/미이행/진행중/완료)
- 테이블 컬럼 : NO, 리포트 유형(색상 배지 — 일간:파랑, 주간:초록, 월간:보라), 리포트 기간, 개선 제안 내용(번호 목록, 행 클릭 시 펼침), 담당자, 상태(배지), 처리일, 처리내역(상세 버튼)
- 처리내역 모달 (2단 구성 + 하단 이력) : 좌측 — 리포트 유형/기간/개선 제안 내용(번호 목록) | 우측 — 담당자, 이행 상태 드롭다운, 처리 메모, 저장 | 하단 — 처리 이력 타임라인
- 더미 데이터 : QA 평가 품질 기반 개선 제안 (스크립트 준수율, FAIL 판정, 교육 권장 등)

[리포트 생성기]

- 포함 섹션 > 종합 그룹에 "전기 개선 이행 현황" 체크박스 + "불러오기" 버튼 추가 (선택 건수 표시)
- REPORT_DATA.improvements를 문자열 배열에서 { content, source } 객체 배열로 변환
- REPORT_DATA.prevImprovements 추가 (불러온 전기 이행 항목 저장)
- IMPROVEMENT_POOL : QA 평가 기반 개선 이행 더미 데이터 7건
- 전기 개선 이행 현황 프리뷰 : 유형, 기간, 개선 제안 내용(번호 목록), 담당자, 상태, 처리 메모 테이블
- 불러오기 모달 : NO(선택 시 ✓ 표시), 유형, 기간, 개선 제안 내용(전체 번호 목록), 담당자, 상태 | 상태/내용 검색 필터 | 행 클릭 선택/해제 | 하단 N건 선택 + 선택 완료
- 개선 제안 편집 : AI/수동 배지, 삭제(×) 버튼, "+ 항목 추가" (수동 추가 모달), "개선 제안 확정" 버튼
- 개선 제안 확정 모달 : 항목 목록(번호 + AI/수동 배지) 표시, 리포트 유형/기간 안내, 취소/확정 버튼
- 개발 시나리오 HTML 주석 추가 (순환 흐름 설명)

**수정 리스트 (25/03/25)**
[대시보드]

- 팀별 평가 현황 항목 선택 시 
통합 평가 현황 > 상세 분석탭 > 팀 댑스까지 이동.
- 팀별 평균 점수 항목 선택 시 
통합 평가 현황 > 상세 분석탭 > 팀 댑스까지 이동.
- 품질 추세, 팀별 평균 점수 : 마우스 호버 시 툴팁 나오는 것을 없애고 그래프 위에 데이터값이 나오게 수정
  - 팀별 평균 점수 (바 차트) : 모든 막대 상단에 점수 표시
  - 품질 추세 (라인 차트) : 데이터 포인트 20개 미만이면 전체 표시, 20개 이상이면 하나 건너 표시
- 점수 분포 : 범례에 건수 추가 (예: 우수 (90점 이상) 32건 47%)
- 관리자 확인 영역 : 건수 배지 폰트 크기 11px → 14px로 확대, 패딩 조정
- 최다 감점 항목 Top5 : 건수 옆에 퍼센트(%) 추가, 색상 검은색(#424242)으로 통일, font-weight 500으로 항목명과 동일하게 맞춤
- 최다 감점 콜 Top5 : 클릭 시 통합 평가 현황 > 상세 분석 > 4댑스(전체/센터/팀/상담사)로 이동. 
- 금월 평가 건수 : 자동/IB, 자동/OB 클릭 시 통합 평가 현황(평가방식+통화유형 필터), 수동 클릭 시 수동 평가 현황으로 이동
- 평균 평가 점수 : 자동/IB, 자동/OB 클릭 시 통합 평가 현황(평가방식+통화유형+점수구간 필터), 수동 클릭 시 수동 평가 현황(점수구간 필터)으로 이동

[통합 평가 현황]

- 통합 평가 결과 테이블 컬럼 추가 : 사번, 신뢰도 컬럼 신규 추가
- 컬럼 순서 변경 : 평가 ID / 일시 / 사번 / 센터·팀 / 상담사 / 상담 유형 / 통화 시간 / 유형 / 평가방식 / 평가표 / 점수 / 신뢰도 / 이슈
- 평가방식 표시 변경 : AI·NLP 구분 → 자동/수동으로 단순화, 필터 옵션도 전체/자동/수동으로 변경
- 신뢰도 표시 : 자동 평가는 High(녹색)/Mid(노란색)/Low(빨간색) 배지, 수동 평가는 '-'
- 테이블 정렬 기능 추가 : 이슈 컬럼을 제외한 11개 컬럼에 오름차순/내림차순 정렬 버튼 추가 (헤더 클릭 시 오름차순 ▲ → 내림차순 ▼ → 초기화 순환)
- 평가 ID, 사번 폰트 통일 : monospace 제거하여 다른 컬럼과 동일한 폰트로 맞춤

[자동 평가 현황]

- 테이블 컬럼 순서 변경 : 평가 ID / 일시 / 사번 / 센터·팀 / 상담사 / 상담유형 / 통화시간 / 유형 / 평가표 / 점수 / 신뢰도 / 이슈 (기존 11개 → 12개)
- 테이블 정렬 기능 추가 : 이슈 컬럼을 제외한 11개 컬럼에 오름차순/내림차순 정렬 (헤더 클릭 시 ▲ → ▼ → 초기화 순환)
- monospace 폰트 제거하여 전체 컬럼 폰트 통일

[수동 평가 현황]

- 테이블 컬럼 순서 변경 : 평가 ID / 일시 / 사번 / 센터·팀 / 상담사 / 상담유형 / 통화 시간 / 유형 / 평가표 / 점수 / 평가자 / 평가 상태 / 이슈 (13개 유지)
- 사번 컬럼 신규 추가, 평가 계획 컬럼 제거 (총 수 동일)
- 상담사를 센터/팀 뒤로 이동, 상담유형·통화시간을 상담사 뒤로 이동, 점수를 평가표 바로 뒤로 이동
- 테이블 정렬 기능 추가 : 이슈 컬럼을 제외한 12개 컬럼에 오름차순/내림차순 정렬 (헤더 클릭 시 ▲ → ▼ → 초기화 순환)
- monospace 폰트 제거하여 전체 컬럼 폰트 통일

[개선 이행 관리]

- 처리내역 모달 > 개선 제안 정보 : 라벨명 변경 (리포트 유형 → 유형, 리포트 기간 → 기간)
- 처리내역 모달 > 처리 관리 > 조치 날짜 : 자동 입력에서 수동 선택 방식으로 변경. 커스텀 날짜+시간 선택기 구현 (기존 pp- 디자인 시스템 활용, 분은 10분 단위 선택)
- 처리내역 모달 > 처리 관리 > 담당자 : 고정 텍스트에서 텍스트 입력 + 검색 버튼으로 변경. 기본값은 로그인 유저명. 검색 버튼 클릭 시 관리자 권한 사용자 목록 검색 모달 표시 (실시간 필터링, 클릭 선택)
- 처리내역 모달 > 처리 관리 > 저장 시 유효성 검사 추가 (조치 날짜, 담당자 미입력 시 안내 메시지)
- 개선 이행 목록 테이블 컬럼명 변경 : 리포트 유형 → 유형, 리포트 기간 → 기간
- 기간 표시 형식 변경 : 주간 "2025-3월 둘째 주", 월간 "2025-3월", 년간 "2025년" (월 앞자리 0 제거)
- 검색 조건 > 기간 필터 : 기존 날짜 입력(input type="date" 2개)에서 공통 기간 선택 컴포넌트(AICC_PeriodPicker)로 변경. 검색 조건 라벨명도 "리포트 유형" → "유형"으로 통일

[개선 이행 관리-개선 제안 관련]

- 유형 컬럼 배지명 변경 : 일간 → 일간 리포트, 주간 → 주간 리포트, 월간 → 월간 리포트, 연간 → 연간 리포트
- localStorage 병합 : 통합 평가 현황 등 외부에서 등록한 조치 항목을 개선 이행 목록에 병합 표시

[통합 평가 현황-개선 제안 관련]

- 검색 조건 필터 동작 변경 : 센터/팀/이슈 유형 드롭다운 선택 시 즉시 필터링 → 검색 버튼 클릭 시에만 필터링 적용으로 변경
- 기간 필터 기능 추가 : AICC_PeriodPicker 선택 값으로 실제 날짜 범위 필터링 적용 (일간/주간/월간/년간/사용자 지정)
- AI 개선 제안 롤링 바 추가 : 검색 조건 영역과 통합 평가 결과 테이블 사이에 위치. 검색 조건 적용 시 표시, 초기화 시 숨김
  - 더미 제안 풀(15개)에서 3~5개 랜덤 선택, 4초 간격 fade+slide 애니메이션으로 자동 교체
- 조치 등록 모달 추가 : 개선 제안 바의 "조치 등록" 버튼 클릭 시 처리 내역 모달 표시
  - 좌측 "개선 제안 정보" : 검색 조건을 "라벨 › 값" 형식으로 줄바꿈 표시 (기간/센터/팀/상담사/점수 범위 등), 검색 결과 건수, 개선 제안 내용 (편집 가능 — 항목별 삭제 x 버튼 + 추가 버튼으로 인라인 입력)
  - 우측 "처리 관리" : 조치 날짜(커스텀 달력), 담당자(검색 모달), 이행 상태, 처리 메모, 저장
- 조치 등록 저장 시 localStorage 연동 : 개선 이행 관리 페이지에 항목 자동 추가 (유형 컬럼에 검색 조건 요약 표시)

[통합 평가 현황 > 상세 분석 탭-개선 제안 관련]

- AI 개선 제안 롤링 바 추가 : KPI 카드 영역과 현황 테이블 사이에 위치. 각 depth(전체/센터/팀/상담사) 진입 시 자동 표시
  - 더미 제안 풀(15개)에서 3~5개 랜덤 선택, 4초 간격 fade+slide 애니메이션으로 자동 교체
- 카드 필터 연동 : 팀(3depth), 상담사(4depth)에서 긴급 이슈/최다 감점 항목/70점 이하 콜 카드 클릭 시 개선 제안 갱신
- 조건 요약 : depth 기반으로 "라벨 › 값" 형식 표시 (센터 › 서울센터 / 팀 › VIP상담팀 등), 카드 필터 활성 시 필터 조건 추가 표시
- 조치 등록 모달 : 전체 탭과 동일한 구조 (날짜 선택기, 담당자 검색, 이행 상태, 처리 메모, 개선 제안 편집 가능)
- 조치 등록 저장 시 localStorage 연동 : 개선 이행 관리 페이지에 항목 자동 추가
- iframe 모달 전체 화면 표시 수정 : 상세 분석 탭(iframe) 내에서 모달이 열릴 때 iframe 영역만 어둡게 처리되던 문제 수정
  - postMessage 기반 부모-iframe 통신으로 전체 화면 오버레이 처리 (크로스 오리진 제한 대응)
  - 부모 페이지(integrated-evaluation.html)에 message 이벤트 리스너 추가하여 iframe 전체화면 확장/복원 처리
  - 모달 열림 시 iframe 내부 콘텐츠(#page-content) 숨김 처리로 배경 콘텐츠가 깨져 보이는 현상 해결
  - 모달 닫힘 시 220ms 딜레이 후 iframe 원래 크기/스타일 복원
- GitHub Pages 환경 iframe 모달 깨짐 수정 : 동일 origin(https) 환경에서 ResizeObserver가 iframe 확장 스타일을 덮어쓰는 문제 해결
  - 원인 : 로컬(file://)에서는 cross-origin으로 ResizeObserver가 비활성, GitHub Pages에서는 same-origin으로 활성화되어 모달 확장 시 iframe 높이를 지속적으로 재설정
  - adjustIframeHeight()에 _iframeModalActive 플래그 추가, 모달 활성 상태에서 ResizeObserver 높이 갱신 차단
  - postMessage 핸들러에서 da-modal-expand 시 플래그 활성화, da-modal-restore 시 플래그 해제

**수정 리스트 (25/03/30)**
[대시보드]

- 점수 분포 카드 개선
  - 범례 텍스트(라벨·건수·퍼센트) 크기: 11px → 14px
  - 도넛 그래프 크기: 160px → 150px
- 관리자 확인 영역 텍스트 크기 조정
  - 이슈 제목(FAIL, 기준 미달 등): 13px → 15px
  - 이슈 설명(팀명 등): 11px → 13px

[통합 평가 현황, 자동 평가 현황, 수동 평가 현황]

- 테이블 컬럼 순서 변경 및 센터/팀 분리 (3개 페이지 공통)
  - "센터/팀" 단일 컬럼 → "센터", "팀" 각각 독립 컬럼으로 분리
  - "사번" 컬럼 위치 변경: 상담사 우측 → 상담사 좌측
  - 최종 컬럼 순서: 평가ID → 일시 → 센터 → 팀 → 사번 → 상담사 → ...
  - 정렬 로직에 "팀" 키 추가, "센터" 정렬은 센터만 기준으로 분리
  - colgroup 너비 재배분 (컬럼 1개 추가에 맞춰 조정)

**수정 리스트 (25/03/31)**
[리포트 생성기]

- 품질 추이 차트 개선
  - 각 포인트 위에 데이터 레이블(정수 값) 직접 표시
  - 마우스 호버 툴팁 비활성화
- 팀별 평균 점수 차트 개선
  - 각 막대 끝에 데이터 레이블(소수점 1자리) 직접 표시
  - 마우스 호버 툴팁 비활성화
- chartjs-plugin-datalabels CDN 의존성 추가
- 점수 분포 레이아웃 정렬 개선
  - 도넛 차트 + 범례 영역을 중앙 정렬로 변경 (좌측 치우침 해소)
  - 범례 텍스트와 건수/비율 간격 고정으로 정렬 통일

[전체 영역]

- 콘텐츠 영역 최대 너비 제한
  - .main-content에 max-width: 1744px 추가 (1920px - 사이드바 176px 기준)
  - 1920x1080 해상도에서는 기존과 동일, 더 큰 모니터에서는 콘텐츠가 무한 확장되지 않도록 제한

**해야 할 일**
자동, 수동 미평가 항목 탭으로 분리할 필요가 있음. 

---

**수정 리스트 (26/05/15)**

[데이터 정합성 풀 전환 — 금융권(ECS증권)]

- QA 모듈 더미 데이터를 시나리오 기반으로 전면 재생성
  - `assets/dummy-data/qa-script-templates.json` 신규 — 18개 QA 시나리오 (계좌개설/상품권유/설명의무/적합성진단/거래지원/기술지원/민원/해피콜/FAIL/신입)
  - `tools/generate_qa_data.py` 신규 — 시나리오 기반 평가 데이터 생성기 (seed 고정 idempotent)
  - `tools/verify_qa_consistency.py` 신규 — KPI ↔ 평가 데이터 정합성 검증 스크립트
  - `assets/dummy-data/evaluations.json` 80건, `manual-evaluations.json` 26건 재생성
  - `assets/dummy-data/dashboard-stats.json` — 모든 KPI 수치를 evaluations 집계로 도출 (FAIL/기준미달/이의대기/팀별 점수 등)
  - `assets/dummy-data/all-data.js` 인라인 동기화 (file:// 환경 대응)
  - `pages/agent/my-evaluations.html` 의 하드코딩 allEvals 15건 블록도 동기화
- 풍부도 강제 기준 (verify로 자동 검증)
  - transcript: 모든 레코드 8턴 이상 (인사→본론→해결→마무리 4단)
  - ai_feedback: 150~280자 (강점+감점항목+카테고리 포커스+코칭 제안)
  - scores[*].reason / evidence: 감점 항목마다 구체 사유 + NLP/AI 탐지 근거 동봉
  - manual: qa_comment(QA 매니저 코멘트) + coaching_plan(신입/저성과자)
- "키움증권" → "ECS증권" 브랜드 전환 (전체 1,015건 일괄 치환, 13개 파일)
- `evaluation-items.json` 의 `required-privacy` 키워드를 증권 도메인으로 보정 (구매자명 → 고객명/생년월일/계좌번호)
- `pages/admin/detail-analysis.html` 의 쇼핑몰 시대 카테고리 54건과 하드코딩 transcript 2건을 증권 시나리오로 치환

[QA 대시보드]

- "관리자 확인 영역" → "이상징후 감지" 라벨 변경
- 최다 감점 콜 Top 5 산출 로직 개선
  - manual pending(평가 미완료) 레코드 제외
  - 욕설 FAIL(0점) 콜 제외 — 이상징후 감지의 FAIL 카드로만 노출
  - 1~79점 구간의 저점수 콜만 후보로 포함
- 이의 제기 대기 카운트 = dispute-inbox.html status='대기' 5건과 정합

[통합 평가 현황 — 전체 탭]

- AI 개선 제안 롤링 바 동작 개선
  - 정적 15개 풀 → 현재 필터링된 결과에서 동적 집계로 변경 (팀 평균/FAIL 발생/기준미달/Top 감점항목/I·O 채널 점수차/우수 콜 비율/통화시간 이상치)
  - 팀명 정규화 (시드 잔재 "기술지원팀" ↔ "HTS기술지원팀" 등) 으로 통계 분리 방지, 최소 표본 5건 이상으로 신뢰성 확보
  - 페이지 진입 시 즉시 노출 + 초기화 후에도 유지 (다른 추천 set으로 갱신)
  - 마우스 hover 시 롤링 일시정지, ⌃/⌄ 화살표로 수동 prev/next 이동, N/M 카운터 표시
- mergeEvaluations 데이터 변조 제거
  - 기존: fail_items 모두 빈배열로 덮어쓰고 issues 에서 FAIL/기준미달 키워드 필터링, total_score 65로 클램핑 → 실 데이터의 FAIL/저점수 콜 사라짐
  - 변경: evaluations.json 의 원본 그대로 사용
- generateRecentEvals 의 가짜 FAIL 3건·기준미달 6건 시드 삽입 코드 제거 (transcript·점수가 정상콜 클론이라 상세 모달에서 "FAIL 표시인데 내용 정상" 현상 발생)
- issue=fail 필터 로직 정확화: `fail_items.length>0` → `scores.profanity.is_fail === true` (감점 항목 목록과 FAIL 판정 의미 충돌 해소)
- issue=under 필터 로직 보강: manual pending 제외, auto + manual completed 합산
- 사번 매핑 확장: EXTRA_AGENTS 19명(agent101~agent702) 사번 추가, 매핑 누락 시 "-" 표시 해소
- 조치 등록 모달의 개선 제안 카드 레이아웃 정리
  - 1줄 텍스트 → [핵심 사실 / 권장 액션] 2단 카드 구조
  - 보라색 원 번호 + 항목 간 구분선 + ×버튼 우측 정렬

[통합 평가 현황 — 상세 분석 탭]

- 4뎁스(전체/센터/팀/상담사) KPI 카드를 모두 8개(2행 × 4개)로 통일
  - L0/L1: 기존 4개 → 평가 상담사/평가 건수/긴급 이슈/평균 통화시간(Row1) + 평가 제외/평균 점수/최다 감점/70점이하(Row2) 추가
  - L3: 기존 6개 → 팀 내 순위(좌측 narrow) + 이의 제기 건수(좌측 narrow 2행)로 8개 완성
- L3 "팀 내 순위" 카드에 비교 지표 추가 — 좌측 큰 숫자(순위) + 우측 2컬럼(팀 평균 ↔ 본인 평균)
  - 본인 평균이 팀 평균보다 높으면 파란색, 낮으면 빨간색으로 즉시 인지
- 4뎁스 정합성 검증 및 보정
  - Level 2 카드 = team 사전 집계값 사용 (10명 표본 기반 actual* 계산 제거)
  - Level 1 카드 = teams 합산 (센터 사전값 대신)
  - Level 0 카드 = centers→teams 말단 합산 (Level 1 합과 자동 정합)
  - dashboardSummary 의존 제거 — DUMMY 트리 단일 진실의 원천
- detail-analysis DUMMY 트리 전면 재생성 (`tools/sync_detail_analysis_dummy.py` 신규)
  - centers.json + users.json + EXTRA_AGENTS + evaluations.json 기반으로 27명 상담사 / 101건 평가 이력 분배
  - 쇼핑몰 시대 팀명(VIP상담팀/기술지원팀/일반상담팀 등) → 현재 마스터 명칭(VIP고객팀/HTS기술지원팀/주식상담팀)으로 동기화
  - qa-dashboard 최다 감점 콜 클릭 → 3뎁스 자동 진입 정상 작동 (이전엔 매칭 실패로 0뎁스로 떨어짐)
- FAIL 콜 항목별 점수 분배 개선
  - 기존: 욕설 탐지 시 전 항목 0점 강제 처리 → "첫인사 잘했는데도 모두 미달" 부자연
  - 변경: profanity 항목만 `is_fail=true` + score=0, 나머지는 시나리오 transcript 흐름에 맞춰 차등 분배 (GOOD_ITEMS 만점 / POOR_ITEMS 30~50% / profanity 0점)
  - 총점 60~70점대로 표시되지만 FAIL 분류는 `is_fail` 플래그로 유지

[상세 분석 탭 하단 통합 평가 결과 + 카드 검색 키워드 연동]

- iframe(detail-analysis) 하단에 통합 평가 결과 테이블 신규 추가
  - 전체 탭과 동일한 14컬럼 + 정렬 + 페이지네이션 + 평가 상세 모달
  - 전체 탭과 독립 상태 (`detailFilteredEvals`/`detailCurrentPage`/`detailSortKey`)
- 4뎁스 모든 카드를 검색 키워드처럼 사용 (Option C — 하단 테이블만 필터링, iframe 내부 테이블은 불변)
  - 정보성 카드(평가 상담사/평가 건수/평균 통화시간/평균 점수/팀내순위/이의제기) 본체 클릭 = Base 노드 전체 콜
  - 작용 카드(긴급 이슈/평가 제외/최다 감점/70점 이하) 클릭 = Base + 해당 조건
  - 평가 건수·평균 점수 카드 sub-cell(자동/IB·자동/OB·수동) 분리 클릭 = Base + 채널/평가방식
- 통신 프로토콜
  - 자식 → 부모 `da-node-change` (드릴다운·브레드크럼 시) → 하단 테이블이 새 노드 base로 갱신
  - 자식 → 부모 `da-card-filter` (카드/sub-cell 클릭 시) → 하단 테이블에 Base + kind 필터링
- 카드 재클릭 시 활성 키 해제 → 노드 base로 복귀
- iframe 내부 카드는 본체 외관 그대로, sub-cell만 hover 강조(`#F5F5F5`) + title 툴팁 표시
- 기존 L2/L3 `filterByCard()` 호출 흐름 제거 (함수는 보존, 호출 없음)

**수정 리스트 (26/05/16)**

[상세 분석 iframe 스크롤 통합]

- 부모-자식 단일 스크롤 — 카드 영역에 별도 스크롤 발생 / 모달 클리핑 / 무한 확장 루프 등 단계적 시행착오 후 안정화
  - 자식(detail-analysis.html, embed 모드)이 `#page-content.getBoundingClientRect().bottom` 으로 자기 콘텐츠 높이만 측정해 부모에 `da-height` postMessage 전송 (file:// 환경의 cross-origin 제약 우회)
  - 부모(integrated-evaluation.html)는 수신 즉시 iframe.style.height 동기화, `Math.max(h, 600) + 16px` 여유 버퍼
  - 자식 측 폴링(50ms~3초 8개 시점) + MutationObserver + ResizeObserver + 5초간 500ms 안전망 인터벌
  - iframe `scrolling="no"` 로 내부 스크롤바 노출 차단
  - iframe 모달(da-modal-expand) 활성 중에는 부모 body/html `overflow:hidden` 으로 배경 스크롤 잠금, 모달 복원 시 원래 overflow 복원
- AICC_Modal 컴포넌트 — `document.body.style.overflow` 만 잠그던 것을 `documentElement` 까지 함께 잠그도록 확장. 이전 값을 보존했다가 close 시 정확히 복원

[데이터 정합성 풀 전환 — 단일 진실의 원천 + 시연 풍부도 확보]

- 기존 정합성 충돌 (대시보드 6956건 vs 통합현황 286건 vs 상세분석 101건) 해소 → 모든 화면이 동일 데이터셋(2790건) 사용
- 평가 데이터 확장 — AUTO_DISTRIBUTION ×30 / MANUAL_DISTRIBUTION ×15 (총 자동 2400 + 수동 390)
- generateRecentEvals 가짜 보강 로직 제거 — evaluations.json + manual-evaluations.json 만 단일 출처로 사용
- build_dashboard 의 인위적 스케일링 제거 — `scale = (agent_count // 2) * 90` 같은 부풀림 제거, 실 record 카운트 기반 산출
- 평가 record에 `employee_id` 직접 박기 — integrated-evaluation 의 EMPLOYEE_ID_MAP / EMPLOYEE_NAME_MAP 의존성 제거 (assignEmployeeId fallback만 유지)
- centers.json `agent_count` (108명) 만큼 자동 충원 — `build_agent_pool` 에 filler 상담사 생성 로직 추가 (이름은 결정론적 한국식 풀에서 추출, 사번은 `_CENTER_PREFIX + tid_seq + seq` 패턴)
- agent_id 충돌 해소 — `_TEAM_SEQ` 7팀 모두 unique prefix 부여
- 모든 팀에 OB·FAIL·기준미달 분포 보장
  - `SCENARIO_TEAM_PREF["form-ob-tele"]` 가중치 조정 + VIP·기술팀까지 OB 분배
  - `CATEGORY_TEAM_BIAS` 의 해피콜·적합성진단·설명의무·상품권유 카테고리에 CS·HTS 팀 보조 추가
  - FAIL 카테고리 특수 처리 — `pick_team_for_template` 가 form 제약 무시하고 7팀 전체에 분배
- sync_detail_analysis_dummy.py — evaluations.json 기반 자동 추출로 변경 (EXTRA_AGENTS 정적 명단 의존 제거), 108명 트리에 정확히 분배
- agent_count vs agentCount 키 표준화 — 팀 객체 7곳 일괄 `agentCount` 로 통일 (이전엔 표시값이 "undefined명" 으로 나오던 버그 해소)
- verify_qa_consistency.py — agent_id 화이트리스트 → 패턴 검사로 완화, 팀 평균 점수 tolerance 0.1 → 1.0 (데모 inject 후 미세 변동 허용)

[KPI 카드 — 검색 키워드 / 필터 정합 강화]

- 비검색 카드 클릭 비활성 — `SEARCHABLE_KINDS = ['urgentIssue', 'topDeduction', 'lowScore', 'excluded', 'durationScope']` 만 클릭 가능, 그 외(평가 상담사/평가 건수 본체/평균 점수 본체) 는 `cursor:default` + `.kpi-card-static` 클래스로 hover 그림자 제거
- 카드별 sub-cell 키 unique 분리 — `autoIB-call` / `autoIB-score` 등 prefix 부여, 클릭 시 같은 키 가진 다른 카드의 sub-cell이 동시 강조되던 문제 해소
- 점수 카드 sub-cell 점수 구간 필터 — 카드의 평균 점수가 속한 버킷(60미만/60-69/70-79/80-89/90+) 만 노출. 필터 배지에 구간 명시
- 평균 통화시간 카드 검색 기능 추가 — 1분 단위 버킷(1분 미만/1-1:59/2-2:59/3-3:59/4-4:59/5분+) 필터링. 돋보기 아이콘 노출
- 카드 카운트 = 부모 필터 결과 정합화
  - `urgentIssues` : 상담사 단위 → record 단위(FAIL profanity record 수) 로 산식 변경
  - `lowScoreCalls` : `s <= 70` → `0 < s < 70` 로 부모 필터와 동일 산식
  - `calls_manual_done` : 센터 집계 시 `= calls_manual` 박혀있던 버그 → 팀별 completed 합산으로 수정
- 자동/수동 채널 분리 정확화 — `to_detail_eval(r, is_manual)` 인자로 type 결정. manual record가 type="AI" 로 잘못 분류되던 문제 해소
- 수동 평가 전체(pending 포함) 카운트 = 부모 manualOnly 필터 결과 정합 — `tobj["calls_manual"]` 에 manual 전체, `calls_manual_done` 에 completed만

[평가 제외 상담사 카드 — 명단 카드로 전환]

- 평가 record에 `excluded=true` 필드 직접 박기 — 각 팀 마지막 1명을 결정론적으로 평가 제외 처리(총 7명, 그 사람들 평가 모두 excluded=true)
- 카드 클릭 시 통합 평가 결과 테이블 숨기고 `detail-excluded-card` 노출 — 상담사 단위 명단 표시 (NO/상담사/사번/센터/팀/제외 사유/제외 시작일/예상 복귀)
- 결정론적 제외 사유 7가지 풀 — 신입 교육 중/육아 휴직/병가 휴직/교육 파견/평가 제외 사유 보유/신규 부서 이동/리더 직무 전환 (agent_id 해시 기반 배정)
- 평가 모순감 해소 — 평가 제외인데 점수·평가표 컬럼 표시되던 위화감 제거

[VIP고객팀 윤민서 — 시연 효과 풍성한 5건 데모 데이터]

- `tools/inject_vip_demo.py` 신규 — 윤민서 최근 5건 평가 record를 다양한 시나리오로 덮어쓰기
- 5개 시나리오 — 비대면 일반계좌 개설(95점) / ELS 21호 설명의무(92점) / 펀드 적합성 진단(78점, 일부 감점) / VIP 포트폴리오 자산배분(88점) / 고령자 보호 절차(90점)
- 각 record — transcript 15~25턴 자연스러운 한국어 증권 도메인 대화, ai_feedback 250~300자 강점/약점/코칭, scores 16개 항목 + 일부 reason, tags 6개, 시나리오별 평가표/상담유형 분기
- manual pending 2건은 completed 로 변환하여 점수 표시 (사용자가 상세 모달에서 풍성한 내용 확인 가능)
- detail-analysis.html `showEvalDetail` 보강 — `window.AICC_DATA` 에서 동일 ID record lookup, 풍성한 데이터가 있으면 transcript/ai_feedback/scores/eval_form/consultation_type 모두 우선 사용 (기존엔 sim 함수가 무조건 동적 생성하던 흐름)
- `convertRichScoresToSim` 헬퍼 + `RICH_SCORE_NAME_MAP` (영문 item_id → 한국어 라벨) 추가
- generate_qa_data.py main 끝에 `inject_vip_demo.main()` 자동 호출 — generate 재실행해도 데모 보강이 사라지지 않음

[L2 이슈 컬럼 — 상담사 단위 산정으로 변경]

- 기존엔 평가 30+건 중 한 건이라도 issue 있으면 모두 동일 "기준 미달" 로 표시되어 12명 전원 동일 이슈 처럼 보이는 문제 해소
- 새 기준
  - 긴급(`urgentIssue=True`, 빨간 배지) : FAIL 1건 이상 또는 평균 < 70 또는 미달 콜 비율 30%+
  - 일반 이슈 : 이슈 콜 1건 이상 (L3와 정합 유지 — 클릭 시 그 콜들 직접 확인 가능)
  - 이슈 없음 : 0건
- L2 테이블 이슈 컬럼 배경 박스 제거 → 차분한 회색 텍스트(#757575)만 표시. 긴급만 빨강 + 작은 배지로 시각 강조
- 분포 — 108명 중 긴급 25명 / 이슈 있음 41명 / 이슈 없음 42명

[브레드크럼 / 사용자 시연 친화 개선]

- L1 진입 시 브레드크럼이 "전체"만 표시되던 문제 — `renderLevel1` 시작에 navStack 자가 보정 + 마지막에 `updateBreadcrumb()` 명시 호출
- "경미한 흔들림 1회 탐지" 모호 표현 → "기준점 대비 경미한 감점 (1회 탐지)" 통일 (전 영역 16,948곳 일괄 교체)

[페이지네이션 적용 — 대용량 데이터 성능 개선]

- evaluation-status.html (자동 2,400건) — 정적 페이지 버튼 → 동적 페이지네이션 (페이지당 10/20/50 셀렉트). 페이지당 슬라이스 후 innerHTML — DOM 행 2400 → 20 으로 120배 감소
- manual-evaluation-status.html (수동 390건) — 동일 패턴 적용. `manualCurrentPage` / `goToManualPage` / `renderManualPagination` 헬퍼
- 필터 적용 시 자동으로 첫 페이지로 리셋, 정렬·필터·페이지 이동 모두 호환

[리포트 생성기]

- "Excel 다운로드" 버튼 → "Docx 다운로드" 로 라벨 변경 (`doDownload('Docx')`)

[기타 사용자 경험 보정]

- iframe 깊이별 KPI 카드 컬럼 폭 자동 조정
- detail-analysis tree 의 팀 객체에서 `agent_count` (snake) → `agentCount` (camel) 표준화. 렌더링 코드(4곳)와 데이터(7곳) 모두 통일하여 "undefined명" 표시 사라짐

**수정 리스트 (26/05/16)**

[QA 대시보드 — 품질 추세 그래프 클릭 정합성]

- 품질 추세 그래프 클릭 시 통합평가현황으로 이동하지만 빈 화면이 노출되던 문제 해소
  - 원인: 그래프가 현재 월 기준으로 가상 일자(`generateMonthTrendData`)를 생성하여 클릭 시 `?date=2026-05-XX` 로 전달되나, 실 평가 데이터는 `2025-01-15 ~ 2025-01-27` 범위만 존재해 매칭 0건
  - 1차 시도: 트렌드를 실 데이터 기준 1월로 표시 → 5월 필터 상태에서 1월 데이터를 보여주는 부조화 발생
  - 최종 해결: `AICC_DataLoader._postProcess` 에 일자 시프트 로직 추가 — 데이터셋 최대일이 오늘이 되도록 모든 `e.date` 를 평행이동(행 간 간격 보존). 두 평가 JSON(`evaluations.json`, `manual-evaluations.json`)이 동일 기준선 공유
- `buildTrendFromEvals(year, month)` 추가 — 시프트된 실 평가 데이터를 일자별 평균 점수로 집계 → 클릭 시 통합평가현황과 100% 정합

[QA 대시보드 — 팀별 평균 점수 채널 토글 시각 표시]

- 전체/I/B/O/B 버튼 클릭 시 그래프만 변할 뿐 선택 상태를 알 수 없던 문제 해소
- `.channel-btn.active` 스타일 추가 — 파란색(#00A3FF) 배경 + 흰색 텍스트로 강조

[통합평가현황 — FAIL(금지어) 정합성 강화]

- 금지어 FAIL 30건 전원 동일 시나리오(`qa-17-profanity-fail` — "짜증나니까") 클론으로 변별력 부족
- 상위 5건 시나리오 다양화 (transcript, consultation_type, ai_feedback, 점수 reason 모두 보강)
  - AE-2025-02382 (정민호/HTS): 로그인 장애 → "짜증나니까"
  - AE-2025-02400 (박지민/주식상담): 신용한도 거절 → "진짜 미치겠네"
  - AE-2025-02383 (권나윤/해피콜): 만족도조사 거절 → "야" 반말 호칭
  - AE-2025-02387 (임예준/CS): 본인확인 지연 → "그것도 모르세요" 비하 발언
  - AE-2025-02394 (선우민/아웃바운드): OB 캠페인 거절 → "어쩌라고" 적대 표현

[통합평가현황 — FAIL 점수·이슈 정합성]

- FAIL 정책상 전 항목 0점 자동 처리지만 실 데이터는 64~67점으로 표시되던 문제 해소
- FAIL 30건 일괄 `total_score = 0`, 개별 `scores[*].score = 0`, `status = 'fail'` 처리
- `issues` 배열을 14개 보일러플레이트 → 4개 FAIL 핵심 항목으로 슬림화 (탐지 사실 / 0점 처리 / 코칭 + 시말서 / 매뉴얼 재숙지)
- 비-Top5 FAIL 25건 이슈 첫 줄을 팀별 시나리오 풀에서 회전 선택 (HTS·CS·주식·해피콜·OB·펀드·VIP 7개 팀, 표현·시각·맥락 다양화). 상세는 보일러플레이트 유지
